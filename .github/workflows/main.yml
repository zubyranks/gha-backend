name: Deploy
on:
  repository_dispatch:
    types: [builds]
  # push:
  #   branches:
  #     - master

jobs:
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Check git-tag
        run: |
          echo "${{ github.event.client_payload.git-tag }}"
