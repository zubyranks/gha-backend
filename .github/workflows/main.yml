name: Workflow2
on:
  # push:
  repository_dispatch:
    types: [builds]

jobs:
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Check git-tag
        run: |
          echo "${{ github.event.client_payload.git-tag }}" > git_tag.txt
          cat git_tag.txt
          printenv
      - name: Replace Deployment tag with latest version 
        run: |
          pwd
          ls -la /home/runner/work/gha-backend/gha-backend/Infra/Terraform/service_yamls 
