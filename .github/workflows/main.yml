name: Deploy
on:
  repository_dispatch:
    types: [builds]
  # push:
  #   branches:
  #     - master

jobs:
  build:
    name: Run API
    runs-on: ubuntu-latest
    steps:
      - name: Build API
        run: |
          echo "Build API"
          echo "${{ github.event.client_payload.git-tag }}"
          # sleep 10s